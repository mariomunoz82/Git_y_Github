GIT BASH

/*** Limpiar la consolla
clear  

/*** Visualizar el directorio en que nos encontramos
pwd

/**crear un archivo
touch hola.txtt

/*** visualizar un archivo 

cat hola .txt

*** Ver los comandos utilizados
history

/*** Repetir un comando (se coloca el codigo del comando con un signo !)
!343

/** Borrar un archivo
git rm hola.txt

/** Para remover el contenido del archivo y no el archivo en si
git rm --cached prueba.txt

/*** Configuracion de usuario y correo 
git config --global user.name "John Doe"
git config --global user.email johndoe@example.com

/**** recuperar archivos borrados del repositorio (tiene que haber hecho commit antes)
git checkout ..

/*****borrar un archivo del repositorio
gir reset nombredelarchivo.extension





